# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @my-auctions/server`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t my-auctions-server`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system my-auctions-server && \
          adduser --system -G my-auctions-server my-auctions-server

COPY apps/server/dist my-auctions-server/
COPY apps/server/package.json my-auctions-server/
RUN chown -R my-auctions-server:my-auctions-server .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix my-auctions-server --omit=dev -f install

CMD [ "node", "my-auctions-server" ]
